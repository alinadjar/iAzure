# parallel-trigger-pipeline.yml
trigger: none
pool:
  name: 'WEB-1'

parameters:
- name: CREATE_PR
  displayName: "Create PR ?"
  type: string
  default: 'NO'
  values:
    - YES
    - NO
- name: SHOULD_RELEASE
  type: string
  default: 'false'
  values:
    - 'true'
    - 'false'
- name: BRANCH
  type: string
  default: 'dev'
  displayName: "Select Branch"
  values:
    - 'dev'
    - 'devops'
    - 'alpha'
    - 'beta'
    - 'master'

jobs:
# - job: Diagnose_Repositories
#   displayName: '🔍 Diagnose Repository Issues'
#   steps:
#   - checkout: none
#   - bash: |
#       #!/bin/bash
      
#       ORGANIZATION="${SYSTEM_TEAMFOUNDATIONCOLLECTIONURI}"
#       PROJECT="${SYSTEM_TEAMPROJECT}"
      
#       echo "========================================="
#       echo "STEP 1: List ALL repositories in project"
#       echo "========================================="
#       REPO_URL="${ORGANIZATION}${PROJECT}/_apis/git/repositories?api-version=6.1"
      
#       echo "Fetching all repositories..."
#       REPOS_RESPONSE=$(curl -s -u ":${GIT_PAT}" \
#         -H "Content-Type: application/json" \
#         "${REPO_URL}")
      
#       echo "$REPOS_RESPONSE" | jq '.'
      
#       # Extract repository names and IDs
#       echo ""
#       echo "========================================="
#       echo "STEP 2: Repository Names and IDs"
#       echo "========================================="
#       echo "$REPOS_RESPONSE" | jq '.value[] | {name: .name, id: .id, remoteUrl: .remoteUrl}'
      
#       echo ""
#       echo "========================================="
#       echo "STEP 3: Check if branch 'dev' exists in each repo"
#       echo "========================================="
      
#       # Loop through each repository and check for dev branch
#       echo "$REPOS_RESPONSE" | jq -r '.value[] | .name' | while read repo_name; do
#         echo ""
#         echo "--- Repository: $repo_name ---"
        
#         BRANCH_URL="${ORGANIZATION}${PROJECT}/_apis/git/repositories/${repo_name}/refs?filter=heads/dev&api-version=6.1"
#         BRANCH_RESPONSE=$(curl -s -u ":${GIT_PAT}" \
#           -H "Content-Type: application/json" \
#           "${BRANCH_URL}")
        
#         BRANCH_COUNT=$(echo "$BRANCH_RESPONSE" | jq '.count')
        
#         if [ "$BRANCH_COUNT" -gt 0 ]; then
#           echo "✅ Branch 'dev' EXISTS in repository: $repo_name"
#           echo "Branch details:"
#           echo "$BRANCH_RESPONSE" | jq '.value[] | {name: .name, objectId: .objectId}'
#         else
#           echo "❌ Branch 'dev' DOES NOT EXIST in repository: $repo_name"
#         fi
#       done
      
#       echo ""
#       echo "========================================="
#       echo "STEP 4: Get Default Branches"
#       echo "========================================="
      
#       echo "$REPOS_RESPONSE" | jq -r '.value[] | .name' | while read repo_name; do
#         DEFAULT_BRANCH=$(curl -s -u ":${GIT_PAT}" \
#           -H "Content-Type: application/json" \
#           "${ORGANIZATION}${PROJECT}/_apis/git/repositories/${repo_name}?api-version=6.1" | jq -r '.defaultBranch')
        
#         echo "Repository: $repo_name - Default branch: $DEFAULT_BRANCH"
#       done
      
#     env:
#       GIT_PAT: $(GIT_PAT)
#     displayName: 'Diagnose repositories and branches'

- job: Trigger_Backend
  displayName: '🚀 Trigger Backend Pipeline'
  steps:
  - checkout: none
    
  - bash: |
      #!/bin/bash
      
      ORGANIZATION="${SYSTEM_TEAMFOUNDATIONCOLLECTIONURI}"
      PROJECT="${SYSTEM_TEAMPROJECT}"
      PIPELINE_ID="111"  # backend pipeline ID
      BRANCH="${{ parameters.BRANCH }}"
      CREATE_PR="${{ parameters.CREATE_PR }}"
      SHOULD_RELEASE="${{ parameters.SHOULD_RELEASE }}"
      
      echo "=== Triggering Backend Pipeline ==="
      echo "Pipeline ID: ${PIPELINE_ID}"
      echo "Branch: ${BRANCH}"
      echo "CREATE_PR: ${CREATE_PR}"
      echo "SHOULD_RELEASE: ${SHOULD_RELEASE}"
      
      # Build the API URL
      URL="${ORGANIZATION}${PROJECT}/_apis/build/builds?api-version=5.1"
      
      # IMPORTANT: Do NOT specify repository - let the pipeline handle it
      # The pipeline manually clones repos based on the BRANCH parameter
      cat > payload.json << EOF
      {
        "definition": {
          "id": ${PIPELINE_ID}
        },
        "parameters": "{\"CREATE_PR\": \"${CREATE_PR}\", \"SHOULD_RELEASE\": \"${SHOULD_RELEASE}\", \"BRANCH\": \"${BRANCH}\"}"
      }
      EOF
      
      echo "Payload:"
      cat payload.json
      echo ""
      
      # Trigger the build using GIT_PAT
      RESPONSE=$(curl -s -X POST \
        -u ":${GIT_PAT}" \
        -H "Content-Type: application/json" \
        -d @payload.json \
        "${URL}")
      
      echo "Response: ${RESPONSE}"
      
      # Extract build ID
      if echo "$RESPONSE" | grep -q '"id":[0-9]'; then
        BUILD_ID=$(echo "$RESPONSE" | grep -o '"id":[0-9]*' | head -1 | cut -d':' -f2)
        echo "✅ Backend pipeline triggered successfully! Build ID: ${BUILD_ID}"
        echo "##vso[task.setvariable variable=BACKEND_RUN_ID;isOutput=true]${BUILD_ID}"
      else
        echo "❌ Failed to trigger backend pipeline"
        if echo "$RESPONSE" | grep -q "message"; then
          ERROR_MSG=$(echo "$RESPONSE" | grep -o '"message":"[^"]*"' | cut -d':' -f2- | tr -d '"')
          echo "Error message: ${ERROR_MSG}"
        fi
        exit 1
      fi
      
      # Clean up
      rm -f payload.json
      
    name: trigger_backend
    displayName: 'Trigger Backend CI Pipeline'
    env:
      GIT_PAT: $(GIT_PAT)

- job: Trigger_Frontend
  displayName: '🚀 Trigger Frontend Pipeline'
  dependsOn: []
  steps:
  - checkout: none
  
  - bash: |
      #!/bin/bash
      
      ORGANIZATION="${SYSTEM_TEAMFOUNDATIONCOLLECTIONURI}"
      PROJECT="${SYSTEM_TEAMPROJECT}"
      PIPELINE_ID="109"  # frontend pipeline ID
      BRANCH="${{ parameters.BRANCH }}"
      CREATE_PR="${{ parameters.CREATE_PR }}"
      SHOULD_RELEASE="${{ parameters.SHOULD_RELEASE }}"
      
      echo "=== Triggering Frontend Pipeline ==="
      echo "Pipeline ID: ${PIPELINE_ID}"
      echo "Branch: ${BRANCH}"
      echo "CREATE_PR: ${CREATE_PR}"
      echo "SHOULD_RELEASE: ${SHOULD_RELEASE}"
      
      # Build the API URL
      URL="${ORGANIZATION}${PROJECT}/_apis/build/builds?api-version=5.1"
      
      # Do NOT specify repository - let the pipeline handle it
      cat > payload.json << EOF
      {
        "definition": {
          "id": ${PIPELINE_ID}
        },
        "parameters": "{\"CREATE_PR\": \"${CREATE_PR}\", \"SHOULD_RELEASE\": \"${SHOULD_RELEASE}\", \"BRANCH\": \"${BRANCH}\"}"
      }
      EOF
      
      echo "Payload:"
      cat payload.json
      echo ""
      
      # Trigger the build using GIT_PAT
      RESPONSE=$(curl -s -X POST \
        -u ":${GIT_PAT}" \
        -H "Content-Type: application/json" \
        -d @payload.json \
        "${URL}")
      
      echo "Response: ${RESPONSE}"
      
      # Extract build ID
      if echo "$RESPONSE" | grep -q '"id":[0-9]'; then
        BUILD_ID=$(echo "$RESPONSE" | grep -o '"id":[0-9]*' | head -1 | cut -d':' -f2)
        echo "✅ Frontend pipeline triggered! Build ID: ${BUILD_ID}"
        echo "##vso[task.setvariable variable=FRONTEND_RUN_ID;isOutput=true]${BUILD_ID}"
      else
        echo "❌ Failed to trigger frontend pipeline"
        if echo "$RESPONSE" | grep -q "message"; then
          ERROR_MSG=$(echo "$RESPONSE" | grep -o '"message":"[^"]*"' | cut -d':' -f2- | tr -d '"')
          echo "Error message: ${ERROR_MSG}"
        fi
        exit 1
      fi
      
      # Clean up
      rm -f payload.json
      
    name: trigger_frontend
    displayName: 'Trigger Frontend CI Pipeline'
    env:
      GIT_PAT: $(GIT_PAT)

# Monitor job
- job: Monitor_Pipelines
  displayName: '📊 Monitor Pipeline Status'
  dependsOn:
    - Trigger_Backend
    - Trigger_Frontend
  condition: always()
  variables:
    backend_run_id: $[dependencies.Trigger_Backend.outputs['trigger_backend.BACKEND_RUN_ID']]
    frontend_run_id: $[dependencies.Trigger_Frontend.outputs['trigger_frontend.FRONTEND_RUN_ID']]
  steps:
  - checkout: none
  - bash: |
      echo "📊 Pipeline Trigger Summary:"
      echo "================================"
      echo "Backend Run ID: $(backend_run_id)"
      echo "Frontend Run ID: $(frontend_run_id)"
      echo ""
      echo "🔗 Backend URL: ${SYSTEM_TEAMFOUNDATIONCOLLECTIONURI}${SYSTEM_TEAMPROJECT}/_build/results?buildId=$(backend_run_id)"
      echo "🔗 Frontend URL: ${SYSTEM_TEAMFOUNDATIONCOLLECTIONURI}${SYSTEM_TEAMPROJECT}/_build/results?buildId=$(frontend_run_id)"
      echo "================================"
    displayName: 'Display pipeline URLs'
    condition: and(succeeded(), ne(variables['backend_run_id'], ''), ne(variables['frontend_run_id'], ''))